// BOOLEANS  True or False

//

const isMarried = true;

// comparison operator < > <= >= ==
console.log(5 == "5.0");
console.log(5 === "5");

//logial operators and && or || not!
// and operator - every condition must be true
// or just needs one condition to be true

// truthy and falsy values

// ade
// string any value string will be true
const myName = "jay";

// numbers any numbers is true positive or negative
// zeo (falsy)

const value = 0.0;
console.log(Boolean(value));
